/* test binary search tree */


#include "tree.h"


int main()
{
        // tree
        tree_t * tree_entry = tree_create();
        tree_init(tree_entry);
        tree_remove(tree_entry);
}
